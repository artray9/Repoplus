name: Deploy to Railway

# Railway автоматически деплоит при пуше в main через GitHub интеграцию.
# Этот workflow отключён чтобы избежать дублирования.

on:
  workflow_dispatch: # только ручной запуск если нужно

jobs:
  # Railway деплоит автоматически — отдельный CI шаг не нужен
  noop:
    name: Railway auto-deploys on push
    runs-on: ubuntu-latest
    steps:
      - run: echo "Railway handles deployment automatically via GitHub integration"
